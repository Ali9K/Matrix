---
- name: Use daemon.json to set Docker registry, DNS, and logging options
  become: yes
  template:
    src: daemon.json.j2
    dest: /etc/docker/daemon.json
    owner: root
    group: root
    mode: '0644'
    backup: yes
  notify: restart docker
  tags:
    - debian
    - docker
    - set-registry